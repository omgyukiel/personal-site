# Pre-minimal redesign archive

This directory preserves the removed home and profile experiences, their source
data and generator, and the assets required to reconstruct them.

It is source history only. The production build writes an allow-listed set of
pages and assets to `dist/`, so this directory must never be included in the
GitHub Pages deployment artifact.
